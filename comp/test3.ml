
let rec scan_pixel x = 


  (* 次に、直接光の各衝突点について、間接受光による寄与を加味する *)
  let p =
    if a () then
      b ()
    else
      c () in

    (* 得られた値をPPMファイルに出力 *)
  print_float rgb.(0); (* Red   *)
  
  print_float rgb.(1); (* Green *)
  
  print_float rgb.(2) (* Blue  *)
in
  rgb.(0) <- 1.0;
  scan_pixel 0
