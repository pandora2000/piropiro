このディレクトリにはシミュレータまたは実機の画像出力結果を保存する。

答えが変わらないはずの変更を行って、答えが変わってしまったまま見逃すのを防ぐため、
変更を加える度diffをとるとよい。

命名法

H = シミュレータならs,実機ならh
F = 出力したsldファイル名(base.sldならbase)
D = 出力した時間(そのときcompにあったa.sとfp.s、コンパイルしたsimulatorを使って出したものとする)
　　をYYYYMMDDhhmm形式で

として

H_F_D

また、最新の画像に容易にアクセスするためにDをlatestとして最新のものをコピーしておくこと